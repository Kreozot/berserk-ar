# План разработки Berserk AR

## Цель прототипа

Сделать мобильное приложение, которое видит расклад карт ККИ «Берсерк» через камеру, распознаёт отдельные карты, подсвечивает их поверх изображения и по нажатию открывает заранее сохранённое изображение распознанной карты на весь экран.

Первый прототип строится только на наборе **«Легенды Лаара» (19 карт)**. Это позволит проверить распознавание и UX на маленьком контролируемом датасете прежде, чем масштабировать решение на другие выпуски.

## Этапы

### 1. Подготовка репозитория и датасета
- Зафиксировать цель и план проекта.
- Добавить изображения всех 19 карт «Легенды Лаара».
- Добавить машиночитаемый каталог карт с ID, названием и локальным путём к изображению.
- Зафиксировать источник изображений и статус прав на них.

### 2. Выбор технического стека
Для первого прототипа выбрана конфигурация:
- Expo SDK 57 / React Native 0.86;
- `react-native-vision-camera`;
- `react-native-fast-opencv` для детекции и perspective correction;
- ORB feature matching для идентификации карт;
- обычный React Native overlay на первом этапе.

ORB и OpenCV рассматриваются как сменные адаптеры. Детали и границы архитектуры зафиксированы в `docs/adr/0001-card-recognition-architecture.md`.

### 3. Camera MVP
- [x] Открыть live camera preview.
- [x] Отрисовать поверх камеры тестовую интерактивную рамку.
- [x] По нажатию на рамку открыть полноэкранное локальное изображение карты.
- [x] Остановить camera preview, пока открыта полноэкранная карта.
- [x] Подключить VisionCamera Frame Output к CV pipeline.

Первоначальный mock UI flow был проверен на физическом Android-устройстве. Теперь mock оставлен только на уровне идентификации карты.

### 4. Детекция карт
- [x] Получать кадры через VisionCamera Frame Output.
- [x] Уменьшать кадр перед CV-обработкой через GPU Resizer.
- [x] Выполнять тяжёлую обработку на отдельном async worklet thread.
- [x] Искать contours и четырёхугольники через OpenCV.
- [x] Переводить углы Frame → Camera → Preview coordinates.
- [x] Рисовать кликабельный overlay по реальным четырём углам.
- [ ] Проверить и настроить thresholds на физических картах при разных фонах/освещении.
- [ ] Добавить perspective correction / homography.
- [ ] Нормализовать найденную карту до фиксированного размера для recognizer.
- [ ] Проверить работу при наклоне, разных расстояниях и частичном перекрытии.

На этом шаге любая найденная карта всё ещё получает mock ID `ll-001`. Геометрическая детекция и идентификация намеренно отлаживаются отдельно.

### 5. Распознавание «Легенд Лаара»
- Реализовать ORB preprocessing/reference cache для 19 эталонных карт.
- Реализовать feature matching найденной нормализованной области с каталогом.
- Возвращать `cardId`, confidence и координаты четырёх углов.
- Добавить порог уверенности и состояние `unknown`, чтобы приложение не показывало неверную карту.

### 6. Трекинг и плавный AR-overlay
- Не выполнять тяжёлое распознавание на каждом кадре.
- Между распознаваниями отслеживать уже найденные карты.
- Стабилизировать координаты рамок, чтобы они не дрожали.
- Корректно обрабатывать появление, перемещение и исчезновение карты.
- Добавить отдельные tracking IDs для нескольких физических копий одной карты.

### 7. Проверка прототипа
Проверить минимум следующие сценарии:
- одна карта;
- несколько карт одновременно;
- карты рядом и частично перекрываются;
- сильный наклон камеры;
- поворот карты;
- блики;
- слабое освещение;
- рука частично закрывает карту;
- похожие по цветам/композиции карты.

Для каждого сценария фиксировать точность распознавания, задержку и субъективную стабильность overlay.

### 8. Масштабирование
После успешного прототипа:
- добавить следующие выпуски;
- автоматизировать импорт изображений и метаданных;
- проверить, как алгоритм распознавания масштабируется с 19 до сотен/тысяч карт;
- при необходимости добавить другой `CardRecognizer` (например embeddings / ML retrieval) без изменения UI и camera pipeline.

## Принципы реализации
- Сначала минимальный работающий прототип, затем оптимизация.
- Computer vision отделён от UI через небольшой стабильный интерфейс.
- Каталог карт не должен быть жёстко зашит в алгоритм распознавания.
- Любой алгоритм распознавания должен уметь возвращать `unknown`.
- OpenCV/ORB-специфичные типы не выходят за границы инфраструктурных адаптеров.
- Измеряем производительность на реальном устройстве, а не только в симуляторе.

## Текущий статус
- [x] Репозиторий создан.
- [x] План разработки подготовлен.
- [x] Добавить 19 карт «Легенды Лаара».
- [x] Добавить каталог/метаданные карт.
- [x] Сравнить варианты технического стека.
- [x] Зафиксировать выбранный стек и сменную архитектуру.
- [x] Добавить базовые `CardDetector` / `CardRecognizer` contracts.
- [x] Создать React Native application scaffold.
- [x] Реализовать Camera MVP UI flow с mock detection.
- [x] Подключить Frame Output и первую OpenCV-детекцию четырёхугольников.
- [ ] Откалибровать детектор на физических картах и добавить perspective normalization.
- [ ] Реализовать ORB-распознавание.
- [ ] Реализовать трекинг и стабилизацию overlay.
